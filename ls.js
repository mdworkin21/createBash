// const ls = process.stdin.on('data', (data) => {
//     const fs = require('fs')
// })

const ls = require('fs')














// ls.prototype.readdir = function(dir, something, callbk){
//   if (err){
//         throw err
//       } else {
//          process.stdout.write(files.join('\n'))
//          process.stdout.write('\nprompt >')
//       }
//     }


module.export = {
  ls
}
